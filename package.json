{
  "name": "oclip",
  "version": "0.0.0",
  "dependencies": {
    "typescript": "^4.0.5"
  },
  "devDependencies": {
    "husky": "^5.0.0-beta.0",
    "lint-staged": "^10.5.1",
    "pinst": "^2.0.0",
    "prettier": "^2.1.2"
  },
  "scripts": {
    "postinstall": "husky install",
    "prepublish": "pinst --disable",
    "postpublish": "pinst --enable"
  }
}
